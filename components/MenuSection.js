import Image from "next/image";

const MenuSection = ({option, optionImage}) => {
    return ( 
        <section>
            <h4>{option}</h4>
        </section>
     );
}
 
export default MenuSection;